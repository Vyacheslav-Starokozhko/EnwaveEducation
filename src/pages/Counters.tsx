import React from 'react';
import {Counter} from "../components/Counter/Counter";

const Counters = () => {
    return (
        <>
         <Counter/>
        </>
    );
};

export default Counters;
